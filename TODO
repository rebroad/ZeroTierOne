= Change findip to not use /network CLI
= Log why PEER_FILE_ACCESS happening (e.g. happens when a node is removed from ZT Central)
= When we remove a peer we should remove it from the stats too
= Create local firewall functionality that mimicks the ZT Central firewall
= if findip finds no result, then at least show which peer recommended the ztaddr
= See if zerotier offers bandwidth management/quotas - we want quotes for ztaddrs and IP addrs (and ensure traffic via PLANETS/MOONS is logged against the ztaddr and its IP addresses)
= Create a way to assign friendly names to ztaddrs (for use in logging and in CLI outputs)
